#pragma once
#include "x86/types/types.hpp"
namespace compiler::x86::reg {
    //Chaitin Briggs graph coloring reg allocator
    class RegAlloc {
    public:

    private:
        void rewrite_instruction() {

        }

        Operand get_alloced_operand(const Operand& operand) {

        }


    };
}